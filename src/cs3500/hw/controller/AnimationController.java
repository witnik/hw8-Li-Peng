package cs3500.hw.controller;

/**
 * this represent an animation controller, which is used by easyanimator to run an anime.
 */
public interface AnimationController {

  //activate the controller, start the model and make view become visible.
  void execute();
}